{
  "name": "@laurohms/fast-worker-pool",
  "version": "1.0.0",
  "description": "A high-performance asynchronous Worker Pool for the Bun runtime, managing CPU-intensive tasks in parallel using native Web Workers.",
  "type": "module",
  "main": "src/worker-pool.ts",
  "module": "src/worker-pool.ts",
  "scripts": {
    "test": "bun test",
    "benchmark": "bun run scripts/benchmark.ts",
    "example": "bun run src/examples/main.ts"
  },
  "keywords": [
    "bun",
    "worker-pool",
    "workers",
    "multithreading",
    "concurrency",
    "performance"
  ],
  "author": "laurohms",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "latest"
  }
}
